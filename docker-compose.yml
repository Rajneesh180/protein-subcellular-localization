services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./checkpoints:/app/checkpoints
    environment:
      - MODEL_PATH=checkpoints/best_model.pth
      - MODEL_NAME=hybrid
